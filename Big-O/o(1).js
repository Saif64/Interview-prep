const box = new Array(100000).fill('box');

const compressBox = arr => {
    console.log(arr[0]);
};

/* ** as we are finding only 1 thing the time complexity becomes
O(1) or constant time complexity */